//  in this case when our function is in another module we can use require
// Even destructuring can be done

// require function is present in node js only its not in js
// Require function is for importing
// const {add , sub} = require("./math.js")
// const math = require("./math.js")


// console.log(math)
// console.log(math(1,2))

// when more than 1 function is there
// console.log(math.add(1, 2))
// console.log(math.sub(3,1))
//  require(./fs) --> it will search in current directory
//  require(fs)--> this will search by default library
